var numero = parseInt("Digite um número");
for ( let contadora = 1; contadora < 11; contadora++) {
    alert(numero +" x "+contadora);
    var contadora = numero*contadora;
    alert("O resultado " + contadora);
}